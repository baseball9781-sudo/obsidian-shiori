# Obsidian Reading Tracker 実装仕様（最終版）

## 目的

AIや自動パイプラインが生成・追記するObsidianノートについて、ノートを開く前に以下を判別できるようにする。

- ○ 未読
- ◐ 途中
- ● 既読
- 無印 = 対象外

ユーザーが普段やることは、

- 途中でやめる → その位置に栞を置く
- 最後まで読む → 最後の文の後ろに栞を置く

だけ。

---

## 1. 状態判定

状態の正はMarkdown本文内の 🔖。

- 🔖 なし → ○ 未読
- 🔖 より後ろに本文あり → ◐ 途中
- 🔖 以降が空白・改行のみ → ● 既読

栞は1ノート1個を前提とする。

複数ある場合は最後の1個を有効とし、それ以前は無視する（自動削除はしない）。

### 栞として無視するもの

以下の中にある 🔖 は読書栞として扱わない。

- frontmatter
- fenced code block
- inline code
- blockquote

### 栞表記

設定で選択可能。

- `🔖`
- `%%🔖%%`（Obsidianコメント記法。Reading Viewで栞自体は不可視）

判定ロジックは両モードで同一。

---

## 2. 追記

既読状態:

```
最後の文章

🔖
```

AIが末尾に追記:

```
最後の文章

🔖

## 追加
新しい内容
```

→ 自動的に ● → ◐

AI・人間の変更判別はしない。更新履歴も管理しない。

**前提**：AIやパイプラインは既存ノートへの新規内容を基本的にファイル末尾へappendする。末尾以外（🔖より前）への挿入は既読のままになるが、これはプラグインの責務外とし、パイプライン側の運用で担保する。

---

## 3. 対象ノート

### フォルダ指定

設定画面で以下を複数指定可能。

- Include folders
- Exclude folders / files

Includeフォルダ追加時、その配下の栞なしノートは自動的に未読扱いになる。**このときファイルの書き換えは発生しない。**

設定画面には対象件数を表示する。

> Research/：312件が対象

### Frontmatter

フォルダ設定より優先。

```yaml
---
reading-track: true
---
```

→ 強制的に対象

```yaml
---
reading-track: false
---
```

→ 強制的に対象外

優先順位:

1. `reading-track: false`
2. `reading-track: true`
3. Include / Exclude設定

これにより、AIに対して「人間が読む必要のあるノートには `reading-track: true` を付ける」と指示できる。

---

## 4. File Explorer表示

ノートを開かなくても状態が分かることを必須とする。

```
○ AI市場調査
◐ ホテルDB改善
● Claude Code運用
  普通のメモ
```

- ファイル名自体は変更しない
- DOMに `data-reading-state` 等の属性を付け、CSS `::before` などで表示する
- 検索・リネーム・並び順に影響させない

---

## 5. ノートを開いたとき

自動ジャンプは**しない**。

ノート上部に状態UIを表示する。

```
◐ 途中
```

- 途中状態の場合、この表示をクリックすると栞位置へジャンプする
- コマンド「栞へ移動」でも同じ動作

---

## 6. Reading Viewの栞表示

途中ノートでは栞位置に、薄い区切りラインを表示する。

```
──────── ここまで読んだ ────────
```

- 🔖 モードでも %%🔖%% モードでも表示する
- %%🔖%% の場合、Markdown上の栞自体はReading Viewでは見えなくてよい（ラインだけが見える）
- **対象はReading Viewのみ。ライブプレビュー（CodeMirror）での表示は本仕様の範囲外**とする。実装が別系統（post-processor と CodeMirror拡張）になり工数が倍になるため。将来の拡張候補として残す

---

## 7. 栞の付け方

### 主操作

Reading View / 編集画面で対象段落を右クリックし、コンテキストメニューの「**ここに栞**」を選ぶ。

- **クリックした行の直後**に栞を置く（2026-08-31変更。旧: 段落の直後）。Reading Viewでクリック行を逆引きできない場合は段落の直後にフォールバック
- 既存の有効栞がある場合は削除して新しい位置へ移動する

状態は栞の位置で自動的に決まる:

- 途中の段落の後ろに置く → ◐ 途中
- 最後の本文の後ろに置く → ● 既読

つまり「ここに栞」だけでも読了できる。

### 補助コマンド（Command Palette）

| コマンド | 動作 |
|---|---|
| ここに栞 | 編集画面: カーソル行の直後。Reading View: 画面に見えている最後のセクションの直後（2026-09-01追加。デフォルトホットキー Cmd+Shift+B） |
| 栞へ移動 | 栞の位置へスクロール |
| 読了にする | ファイル末尾へ栞を移動 |
| 未読に戻す | 栞を削除 |
| 対象外にする | frontmatterへ `reading-track: false` を追加。栞は削除しない |

ショートカットはユーザーが設定可能。

---

## 8. Readingクエリ

専用Viewは作らない。Markdown内で以下のコードブロックを使えるようにする。

````
```reading
state: unread,reading
folder: Research/
sort: mtime desc
limit: 20
group: true
```
````

`registerMarkdownCodeBlockProcessor("reading", ...)` で実装。

### パラメータ

| キー | 値 | デフォルト |
|---|---|---|
| `state` | `unread` / `reading` / `read` / `all`（カンマ区切りで複数可） | `all` |
| `folder` | 絞り込むフォルダ | 対象フォルダ全体 |
| `sort` | `mtime` / `ctime` / `name` + `asc` / `desc` | `mtime desc` |
| `limit` | 件数上限 | なし |
| `group` | 状態ごとに見出しを付けるか | `false` |

### 動作

- リストは描画のたびに計算する。ファイルには何も書き込まない
- 各項目は内部リンクとして表示する
- クリックで開く

### 用途例：読むもの.md

````
```reading
state: unread,reading
group: true
sort: mtime desc
```
````

---

## 9. 初回既読整理

既存ノートをまとめて整理できるようにする。最低限:

- 選択ノートを既読にする
- 選択ノートを未読にする
- フォルダ単位で既読にする

既読化は末尾に 🔖 を追加する。未読化は 🔖 を削除する。

**実行前に対象件数を表示し、確認を取ってから書き換える。**

> Research/ 配下 287件を既読にします。実行しますか？

本仕様で複数ファイルを一括で書き換えるのはこの機能だけ。実行前のgitコミット（またはバックアップ）を推奨する旨を確認ダイアログに添える。

---

## 10. ファイル書き込みの原則

ここで言う「書き込み」はノートのMarkdown本文（およびfrontmatter）の書き換えを指す。ファイル名・タイトルは一切変更しない。

書き込みが発生するのは以下の2箇所**のみ**:

1. 7章のコマンド・右クリック操作の実行時
2. 9章の一括操作の実行時（確認後）

以下では**一切書き込まない**:

- フォルダ設定の追加・変更（3章）
- File Explorer等の状態表示（4章）
- ノート上部の状態UI表示（5章）
- 「ここまで読んだ」ラインの描画（6章）
- readingクエリの描画（8章）
- 栞位置へのジャンプ

ユーザーの明示的な操作なしにプラグインがノートを編集することはない。

---

## 11. キャッシュ

状態の正は常にMarkdown。高速化のため、以下程度のキャッシュは保持してよい。

```
{
  path: {
    mtime,
    state
  }
}
```

- `mtime` 一致なら再読込しない
- `vault.on("create")` / `vault.on("modify")` / `vault.on("rename")` / `vault.on("delete")` で必要箇所だけ更新
- 起動時にVault全文を毎回スキャンしない。表示・クエリが要求した分から遅延評価する
- 対象判定は先に `MetadataCache` のfrontmatterで絞り込む（本文読み込みより安い）
- キャッシュ（`data.json`）を消してもMarkdownから完全に復元できること

---

## 12. 技術

通常のObsidian Community Pluginとして実装。

- TypeScript / Obsidian Plugin API
- MetadataCache / Vault / Workspace
- `registerMarkdownCodeBlockProcessor`
- DOM decoration / MutationObserver / CSS

### 分離の原則

File ExplorerなどのDOM拡張は公式APIがなく、Obsidian本体の更新で壊れうる。表示箇所ごとに独立した実装とし、**DOM拡張が失敗しても、状態判定・コマンド・readingクエリは動作し続けること**。DOM拡張内の例外は握りつぶし、コア機能に伝播させない。

### 実装上の注意

Reading Viewでの右クリック「ここに栞」は、表示上の段落からソース行番号への逆引きが必要（`MarkdownPostProcessorContext.getSectionInfo()` を使用）。ここが本実装の技術的な山場なので、**最初に単体で動作確認してから他機能に進むこと**。

---

## 完成条件

最低限、以下が動けば完成。

1. 対象ノートを指定できる（folder / frontmatter両方）
2. File Explorerで ○ / ◐ / ● が見える
3. 右クリック「ここに栞」で栞を移動できる（Reading View / 編集画面の両方）
4. 最後の本文後に栞を置けば ● になる
5. AIが末尾に追記すると自動で ◐ になる
6. ノート上部の「◐ 途中」クリックで栞へ飛べる
7. Reading Viewで「ここまで読んだ」ラインが見える（ライブプレビューは対象外）
8. readingコードブロックで未読・途中一覧を作れる
9. 一括既読化が件数確認つきで動く
10. コマンド実行・一括操作以外でファイルが書き換わらない
